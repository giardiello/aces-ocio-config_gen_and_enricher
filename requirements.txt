# Core dependencies for ACES OCIO Config Generator
# Install with: pip install -r requirements.txt

# OpenColorIO Python bindings (>= 2.4 required for ACES 2.0 CLF baking)
opencolorio>=2.4

# Numerical processing (LUT generation, verification)
numpy>=1.24

# Requests (enricher downloads transforms.json from GitHub)
requests>=2.28

# YAML parsing (vendor extensions family manifests)
pyyaml>=6.0

# Schema validation (vendor extensions family manifests)
pydantic>=2.0
